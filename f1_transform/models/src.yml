version: 2
sources:
  - name: raw
    schema: raw
    tables:
      - name: fastf1_laps
        columns:
          - name: compound
            tests: [not_null]
          - name: year
            tests: [not_null]
